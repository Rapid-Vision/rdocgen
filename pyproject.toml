[project]
name = "rdocgen"
version = "0.1.0"
description = "Add your description here"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
rdocgen = "rdocgen:main"

[build-system]
requires = ["uv_build>=0.9.10,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pytest>=9.0.2",
    "ruff>=0.14.14",
]
